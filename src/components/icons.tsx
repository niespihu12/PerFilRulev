import type { LucideIcon } from 'lucide-react';
import { Home, Clapperboard, PiggyBank } from 'lucide-react';

export const categoryIcons: { [key: string]: LucideIcon } = {
  Needs: Home,
  Wants: Clapperboard,
  Savings: PiggyBank,
};
